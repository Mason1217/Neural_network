#ifndef UTILITIES_H
#define UTILITIES_H

#include <stdio.h>

/**
 * @return 0 when failed; otherwise when succeeded.
 */
int read_int(int *number);

/**
 * @return 0 when failed; otherwise when succeeded.
 */
int read_float(float *number);

#endif